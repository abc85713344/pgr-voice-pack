# 普通权限播放器更新验证

版本：2026.9.24.3（2026.09.24-normal-user-input）

本次更新播放器程序，章节配音包和 OCR 组件保持原样。

- 程序继续使用 asInvoker 清单，直接双击时不主动请求管理员权限。
- 增加 Raw Input 实体键盘后备监听。低层钩子与后备监听共用按键状态，同一次按下只推进一次；游戏原有按键仍由游戏处理。
- 设置页显示播放器权限；绑定游戏后检测游戏是否以管理员权限运行。
- OCR 按钮不再仅因无法将管理员游戏切到前台就拒绝截取可见的游戏画面；识别候选仍需玩家确认。
- 已有管理员权限的播放器占用单实例入口时，普通权限启动会给出明确提示，不再直接闪退。

验收：核心逻辑 73 项通过；新版 ZIP 独立解压后，界面 3 项、改键与跟随 6 项、分支 8 项、截图 4 项通过。另使用 Windows 中等完整性级别（S-1-16-8192）的令牌实际启动新版播放器，改键与跟随 6 项、分支 8 项、截图 4 项均通过，记录在本目录“验证记录”的三个普通权限文件中。全部 42 个发布 ZIP 的 SHA256 清单重新核验。新版播放器 ZIP 已更新；两个常用播放器目录的 PgrVoice.exe 也已同步更新。旧版 ZIP 与 EXE 保存在 F:\战双配音包\旧版备份。

限制：本次普通权限测试的模拟游戏窗口同样是普通权限。真正的管理员权限游戏与普通权限播放器并行时，Windows 可能限制按键读取或拦截；这一组合尚未实机通过，不能宣称所有游戏环境都已完全解决。软件会显示权限差异，鼠标面板仍可操作；若游戏也以普通权限运行，已验证的跟随与菜单流程可用。
