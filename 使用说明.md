# 剧情配音播放器

版本：2026.09.24（普通权限键盘跟随修复）

Windows 10/11，64 位电脑。程序自带运行环境，无需安装 Python 或 .NET。

网络分享与完整首次使用步骤见[配置要求与使用指南](配置要求与使用指南.md)。OCR 在 CPU 上运行；建议在游戏以外预留约 1 GB 可用内存。若提示缺少 `VCRUNTIME` 或 `MSVCP`，按指南中的微软官方链接安装 Visual C++ x64 运行库。

## 下载与打开

1. 下载并解压 `01-播放器-Windows-x64.zip`。
2. 下载需要的章节 ZIP，解压到同一个位置。合并同名的“战双剧情配音播放器”文件夹。
3. 需要截图定位时，再解压 `02-OCR组件-可选.zip`，同样合并到这个文件夹。
4. 双击里面的 `PgrVoice.exe`。请先完整解压，不要在压缩包预览里运行。

正确结构如下，留意不要多套一层同名文件夹：

```text
战双剧情配音播放器/
  PgrVoice.exe
  Assets/
  ocr/                     ← 可选
    PgrOcr.exe
    models/
    _internal/
  配音包/
    第29章_源解信标/
      pack.json
      audio/
    第30章_镜像星尘/
      pack.json
      audio/
```

首次打开会发现程序旁的“配音包”目录；也可在设置里选择其他配音包总目录。选择章节、小章节，再在设置中绑定游戏窗口。首次使用确认推进键与游戏一致。启动、恢复进度和浏览台词都不会自行播放。

播放器直接双击以普通权限运行即可。新增实体键盘后备监听：即使原有监听收不到游戏前台按键，也会尝试跟随；两个监听同时收到时不会重复推进。如果游戏以管理员权限运行，Windows 仍可能限制普通播放器读取或拦截按键，尤其是菜单上下键。遇到这种情况，可以二选一：先让游戏也以普通权限运行，或者反过来右键 `PgrVoice.exe` 选择“以管理员身份运行”，让播放器与游戏权限一致。**关键是两边权限保持一致**——一边管理员、一边普通时按键往往就会失效。期间可用鼠标点击配音菜单和面板按钮，截图定位前让游戏画面完整可见。

## 手动选择分支

按 **F1**，或点击“手动选分支”，查看当前小节的人物、话题和剧情菜单。上下键选择，回车打开；随后在菜单中确认与游戏相同的选项。

- 打开菜单和浏览都保持静音。确认具体选项后，进入已核实正文并播放首句；选项直接通往另一个菜单时只打开菜单。
- 游戏已到某个菜单时，可以直接定位到那里，无需重读软件漏记的前置剧情。软件不会因此伪造任务已完成状态。
- 选错后按 **F3** 重选最近一次分支；需要换人时再按 F1。也可使用面板中的“返回话题”和“撤销纠偏”。
- Esc 收起菜单并保持待选，再次打开保留所选位置。
- 正文未核实的内容仅允许单句确认；可靠段落到未知出口仍会停住。这时可手动选当前菜单、F9 定位，或手动选择续接句。

软件仅同步配音位置，游戏内的选择仍由玩家操作。鼠标照常点击游戏，上下键和回车操作打开的配音菜单。

## OCR 定位

安装可选 OCR 包后，按 **F9** 并启用定位。OCR 只在需要时运行，识别过程不会播放。

- 可识别战斗字幕、普通对白和分支选项；搜索范围为当前选中的小章节。
- 候选包含尚未选择路线中的台词。重复句会列出路线和上下文，需选择与游戏一致的位置。
- 确认台词后才播放；确认菜单只打开选项。
- 找不到时，检查小章节是否正确、台词是否完整显示，并展开“查看识别到的文字”。仍可用 F1 或手动选句。
- 原声模式中禁止播放。先按 F8 结束原声时段，明确确认续接位置后再恢复。

## 默认按键

| 按键 | 操作 |
|---|---|
| 空格 | 跟随游戏推进一句 |
| PageUp / PageDown | 配音上一句 / 下一句 |
| F1 | 手动选择本节分支 |
| F2 | 历史与试听 |
| F3 | 重选最近分支 |
| F4 | 章节目录 |
| F5 | 重播当前句 |
| F6 | 展开 / 收起面板 |
| F7 | 暂停 / 恢复跟随 |
| F8 | 进入原声 / 选择续接 |
| F9 | 按需 OCR 定位 |
| ↑↓ / Enter / Esc | 菜单浏览 / 确认 / 收起 |

按键可在设置中修改。旧用户已自定义的按键保持原样；如 F1 已被其他功能占用，可给“手动选择分支”另设快捷键。选中、悬停、滚动和拖动都不会播放。

## 更新与问题检查

更新程序时关闭播放器，替换程序文件即可。用户设置、按键、音量和分章进度保存在 Windows 用户目录，分享包不含制作者的个人进度。

只更新章节时替换相应章节文件夹。章节包记录的是发布时的已核验录音；未通过核验的补配不会作为成功录音加入。缺音、正文待核对和未知路线会在程序中继续提示。

游戏建议使用窗口或无边框模式。OCR 未安装或失败时，手动播放及手动分支仍可使用。启动失败时查看 `%LOCALAPPDATA%\PgrStoryVoice` 下的日志；设置中也可导出诊断。

## 素材与组件

本工具为非官方剧情配音辅助软件。主题插画为战双帕弥什官网“灰鸦”壁纸；出处保存在 `Assets/artwork-source.json`。剧情正文依据 BWiki 和库街区剧情资料，章节包保留来源记录。第三方程序组件及许可见“第三方组件说明.md”和“licenses”文件夹。
