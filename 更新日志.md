# 更新日志

面向玩家的版本变更记录。每一项都能对应到程序里的行为变化。

下载始终在 [Releases](https://github.com/abc85713344/pgr-voice-pack/releases/latest)；
**章节配音包不受版本更新影响，下载过的不必重下。**

---

## 2026.09.27（`2026.09.27-click-zone`）

这一版修了两个跟随相关的问题，并新增了鼠标点击跟随。章节包与 OCR 组件没有改动。

### 修复：按一次「下一句」却前进了两句

上一版为了兼容普通权限，在原有的键盘钩子之外加了一路实体键盘后备监听。两路会同时上报同一次按键，
靠“这个键还没松开就忽略第二次”去重——但开始播放音频会占用界面线程约 0.15 秒，
如果你在这段时间里松手，“松开”会先被处理并把去重状态清掉，随后到达的后备上报就被当成了新的一次按下。

现在两路监听按来源分别记录每个键最近一次的处理时刻，另一路在 800 毫秒内处理过同一个键就忽略。

同时修好的同类表现（同一个根因）：「上一句」一次退两句；「暂停/跟随」暂停后立刻恢复；
面板键展开又立刻收起；「游戏原声」进入后立刻退出。

连按同一个键仍然每次生效，长按只算一次，游戏以管理员权限运行、只剩后备监听时也照常工作。

### 修复：按空格时“软件动了、游戏没动”

低层键盘钩子必须在播放器的界面线程上执行，而界面线程被“开始播放”占用时，系统得等钩子返回
才会把这次按键交给游戏；等得太久，游戏就漏掉了这次按键，而播放器还能从后备路径收到。

现在开始播放和停止播放都移到后台线程串行执行，界面线程立刻返回；低层钩子也只对 ↑ ↓ Enter Esc
询问菜单状态，其他按键不再多做一步。

### 新增：鼠标「下一句」点击热区

键盘跟随之外多了一种更像手感的跟随方式。设置页勾选「显示「下一句」点击热区」后，
屏幕上会出现一个和悬浮球同尺寸同形状的圆角框：

- 拖到游戏里“点击继续”的位置。**游戏在前台时它自动穿透**，点击原样落到游戏里；
  切到桌面或展开面板时它自动变成可拖动，随拖随放。
- 之后点这个框：**游戏照常推进，配音同时跟上**。
- 点框以外的任何地方（选项、技能、地图）都不会跟随，不会再误推进。

### 验证

- 作者自带的验收测试（界面、体验、分支、按键）结果与未修改的原版逐字一致。
- 鼠标探针：左键 / 右键 / 中键在 Raw Input 路径下识别正确。
- 热区探针：尺寸、命中判断、穿透切换全部通过（含 225% 高 DPI 环境）。
- 发布 ZIP 独立解压后单文件运行通过。

### 下载

| 文件 | 大小 | 说明 |
|---|---:|---|
| `01-player-windows-x64.zip` | 69.92 MiB | **必需**。本版播放器 |
| `02-ocr-optional.zip` | 139.9 MiB | 可选，内容与上一版相同 |

SHA256：`20c03cef119dc135c87728a8273702446eab64685c51dfd36f3e3018edaefd30`

---

## 2026.09.24（`2026.09.24-normal-user-input`）

首个公开版本。Windows x64 免安装，覆盖第 3—42 章共 40 个独立章节配音包。

- **播放器**：以普通权限运行，不主动请求管理员权限；新增实体键盘后备监听，跟随游戏逐句推进，不主动播放。
- **手动分支**：按 F1 打开当前小节的完整分支目录，可按游戏当前画面直接定位，不要求重读前置剧情。
- **可选 OCR**：按 F9 截图定位，识别战斗字幕、普通对白和分支选项；候选包含未选路线中的台词，需玩家确认后才播放。
- **章节包**：第 3—42 章各一个独立 ZIP，共 129,320 个章节内引用音频文件（跨章未去重）。

修复：OCR 原先过滤掉未选择路线的台词，现在会展示候选；战斗字幕拆成左右文字块时按同行从左到右拼接；
OCR 支持用选项文字找菜单；F1 可定位当前小节任意有效菜单；F1 在游戏前台、主面板仍展开时也可打开分支目录；
安装包自动发现程序旁的「配音包」目录。
